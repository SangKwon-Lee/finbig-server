{
  "routes": [
    {
      "method": "GET",
      "path": "/email-auths",
      "handler": "email-auth.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/email-auths/count",
      "handler": "email-auth.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/email-auths/:id",
      "handler": "email-auth.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/email-auths",
      "handler": "email-auth.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/email-auths/:id",
      "handler": "email-auth.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/email-auths/:id",
      "handler": "email-auth.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
